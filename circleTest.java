class circleTest
{

	public static void main(String s[])
	{	
		circle c1=new circle();// implicit call to the no argument constructor(default)
		circle c2=new circle();
		c1.setRadius(10.4f);
		c1.findPerimeter();
		c1.findArea();
		c1.showValues();
		factorial.main(s);
	}
}